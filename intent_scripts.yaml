  Welcome:
    speech:
      text: Что вам угодно?
      
  Default:
    speech:
      text: Извините, я некомпетентен
  
  Temperature:
    speech:
      text: Температура {{where}} {{states('sensor.temperature')}} градусов. Влажность {{states('sensor.humidity')}} процентов

  # TurnLights:
    # speech:
      # text: Свет в {{ Room }} {{ OnOff }}
    # action:
      # - service_template: "script.light_plants_{{ OnOff }}"
        
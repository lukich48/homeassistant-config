  default_view:
    view: yes
    icon: mdi:home
    entities:
      - group.all_lights
      - sun.sun
      - group.devices_tracker
      - sensor.Humidity
      - sensor.Temperature

  automations:
    name: Automations and scripts
    view: yes
    icon: mdi:bike
    entities:
      - group.all_automations
      - group.all_scripts

  # media:
    # name: Медиа
    # view: yes
    # icon: mdi:play-circle-outline
    # entities:
      # - group.media_player

  light_plants:
    - light.switch2

  # media_player:
    # - media_player.vlc

  devices_tracker:
    - device_tracker.mqtt_GalaxyS7
    - device_tracker.mqtt_Iphone

